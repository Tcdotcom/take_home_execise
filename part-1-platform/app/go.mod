module github.com/maltego/golden-path-demo

go 1.22

// No external dependencies — the demo app uses only the Go standard library.
// In a real service you would add dependencies here (e.g. a router, metrics client, etc.).
